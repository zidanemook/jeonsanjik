const assert=require('node:assert/strict'),fs=require('node:fs'),os=require('node:os'),path=require('node:path'),vm=require('node:vm');const{build}=require('./build-config.cjs');
const dir=fs.mkdtempSync(path.join(os.tmpdir(),'chagog-config-'));
try{assert.throws(()=>build('',dir),/Missing/);assert.throws(()=>build('no json',dir),/Invalid/);assert.throws(()=>build('{}',dir),/Missing/);assert.equal(fs.existsSync(path.join(dir,'firebase-config.js')),false);const config={apiKey:'test-key',authDomain:'example.firebaseapp.com',projectId:'test-project',appId:'test-app',unrelated:'excluded'};build(JSON.stringify(config),dir);const sandbox={};vm.runInNewContext(fs.readFileSync(path.join(dir,'firebase-config.js'),'utf8'),sandbox);assert.equal(JSON.stringify(sandbox.STUDY_FIREBASE_CONFIG),JSON.stringify({apiKey:config.apiKey,authDomain:config.authDomain,projectId:config.projectId,appId:config.appId}));console.log('PASS deployment config: missing/invalid secret fails, runtime config generated, unrelated fields excluded');}finally{fs.unlinkSync(path.join(dir,'firebase-config.js'));fs.rmdirSync(dir);}
